This version modularizes and generalizes the methods in PlayerInterface, particularly the run() method. It adds a getANumber() method to simplify all the methods that ask for numbers.

The board class implements a method to reveal all the adjacent 0 count cells recursively.